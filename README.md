# 🕐 Лабораторная работа 1 — Паттерн "Наблюдатель" (Observer)

Десктопное JavaFX-приложение: сервер времени раз в секунду уведомляет наблюдателей — три компонента отображают время, воспроизводят музыку по циклу и рисуют фигуру по секунде.

---

## ✨ Возможности

- **Компонент 1** — метка "Время: N сек", обновляется каждую секунду
- **Компонент 2** — цикл пауза M сек + воспроизведение 10 сек (MP3 из `/audio/sample.mp3`); кнопки "Вкл" и "Выкл"
- **Компонент 3** — фигура по `state % 10`: 0 — пусто, 1 — точка, 2 — линия, 3+ — многоугольник; кнопки "Вкл" и "Выкл"
- **Верхняя панель** — Старт / Стоп / Сброс таймера, метка "Состояние времени: N сек (активен/неактивен)"
- **Паттерн Observer** — Subject (TimeServer) уведомляет наблюдателей через `update()`; пассивная модель (наблюдатель сам запрашивает `getState()`)

---

## 🛠 Технологии

| Технология | Версия |
|------------|--------|
| Java       | 17+    |
| JavaFX     | 21     |
| Maven      | 3.13    |

---

## 📦 Запуск

**Требования:** JDK 17+, Maven.

```bash
# Запуск приложения
mvn clean javafx:run
```

Через IDE: запустить класс `org.kp.lab1.MainApp`. Окно 800×600. Для музыки в компоненте 2: положить `sample.mp3` в `src/main/resources/audio/`.

---

## 📁 Структура проекта

```
src/main/java/org/kp/lab1/
├── MainApp.java         # Точка входа, загрузка FXML, Stage
├── MainController.java  # Контроллер main-view.fxml, создание компонентов
├── IObserver.java       # Интерфейс наблюдателя (update)
├── Subject.java         # Интерфейс субъекта (attach, detach, getState, isRunning)
├── TimeServer.java      # Сервер времени — реализация Subject
├── ComponentOne.java    # Наблюдатель: метка времени
├── ComponentTwo.java    # Наблюдатель: аудио по циклу пауза + 10 сек
└── ComponentThree.java  # Наблюдатель: фигура по state % 10

src/main/resources/org/kp/lab1/
├── main-view.fxml       # Вся разметка окна
└── audio/sample.mp3     # Аудио для компонента 2 (опционально)
```

---

## 🎮 Управление

| Действие             | Описание |
|----------------------|----------|
| «Старт»              | Запуск таймера (счётчик секунд каждую секунду) |
| «Стоп»               | Остановка таймера |
| «Сброс»              | Остановка и обнуление счётчика |
| «Вкл» (компонент 2)  | Подписка на сервер (если отписан) и запуск музыки |
| «Выкл» (компонент 2) | Отписка от сервера и остановка музыки |
| «Вкл» (компонент 3)  | Подписка и включение отображения фигуры |
| «Выкл» (компонент 3) | Отписка от сервера, фигура перестаёт обновляться |

---

## 📄 Лицензия

Проект создан в учебных целях.
